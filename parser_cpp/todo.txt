[all] added ** as ^ pow function
[all] added octal numbers
[php] added php parser
[all] testcases are extended
[all] improved Node::calculate function
[all] fixed bug for testcases comparing results Nan, <9e-16
[c++] remove references to aslov library for parser
[c++] adopted debug mode for 64bit compiler
[c++, java] fixed bug with compile with variables vector 
[java] fixed bug with repeated variable

DONE
_DEBUG => !NDEBUG
for (it = m_vnode.begin(); it != m_vnode.end(); it++) {
	delete *it;
}
before any include
#define _USE_MATH_DEFINES
double Node::random() {
	std::lock_guard<std::mutex> lock(m_mutex);
	std::random_device rd;
	std::default_random_engine eng(rd());
	std::uniform_real_distribution<double> distr(0, 1);
	//return distr(eng);
	double v=distr(eng);
	return v;
}
inline Node* createNode(OPERATOR_ENUM _operator, Node *left!!!=NULL!!!) {
	Node *node = new Node();
	node->init(this, _operator, 0, left);
	return node;
}
https://en.cppreference.com/w/cpp/numeric/math
tanh,sinh...
CONSTANT_NAME OPERATOR => vector?
getIndex => add throws string argument
m_operator = (OPERATOR_ENUM) getIndex(OPERATOR, token.c_str());
i = unsigned(m_operator);
if (i < unsigned(OPERATOR_ENUM::SIN) || i > unsigned(OPERATOR_ENUM::MAX)) {
	throw std::invalid_argument("invalid argument exception");
}
//using getIndex
if (std::find(begin(reservedWords), end(reservedWords), s)
		!= end(reservedWords)) {
	throw std::runtime_error(
			"reserved word \"" + a + "\" is used as variable");
}
change function
double Node::calculate() 
#ifndef NDEBUG comment??
+js rarely functions as c++
remove aslov references move to new Project
java new testcases
java rarely functions as c++ 
new testcases for ** ^ octal like binary
add ** ^ octal to js,java
========================================

version history database
add ref to github on pages
calculator,graph new version
update calculator,graph c++&java

??remove 2nd string in testcase

??//toString() already defined in aslov.h so use another name
std::string etoString(Result::ErrorCode c) 

js 1&2
calculator.js on site

+format all files


